# Product pages

Static product detail pages are intentionally server-readable HTML, not JavaScript-only shells. Each page includes a canonical URL, a compact Product JSON-LD block, visible normalized specifications, source provenance and a route back to the compatibility finder.

This directory will be expanded from the initial high-value product pages to the full verified catalog. Merchant prices are stored separately as timestamped observations in `data/offers.json`.
